{
  "name": "format-forge",
  "version": "0.1.4",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "description": "A private Chrome file format converter for DOCX, PDF, images, spreadsheets, audio, video, CSV, JSON, and more.",
  "repository": {
    "type": "git",
    "url": "https://github.com/GlossityLabs/file-format-converter.git"
  },
  "homepage": "https://github.com/GlossityLabs/file-format-converter#readme",
  "bugs": {
    "url": "https://github.com/GlossityLabs/file-format-converter/issues"
  },
  "scripts": {
    "dev": "vite",
    "dev:companion": "tsx companion/server.ts",
    "dev:desktop": "npm --prefix desktop run dev",
    "companion:token": "tsx companion/token.ts",
    "smoke:companion": "node scripts/smoke-companion.mjs",
    "build": "npm run typecheck && vite build && npm run build:companion && npm run verify:build",
    "build:companion": "tsc -p tsconfig.companion.json",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.companion.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify:build": "node scripts/verify-build.mjs",
    "verify": "npm run test && npm run build",
    "verify:desktop": "npm --prefix desktop test",
    "package:extension": "npm run build && node scripts/package-extension.mjs",
    "package:desktop": "npm --prefix desktop run package:mac"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "lucide-react": "^1.24.0",
    "pdf-lib": "^1.17.1",
    "pdfjs-dist": "^6.1.200",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "@types/chrome": "^0.2.2",
    "@types/node": "^26.1.1",
    "@types/react": "^19.0.3",
    "@types/react-dom": "^19.0.2",
    "@vitejs/plugin-react": "^6.0.3",
    "jsdom": "^29.1.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=20"
  }
}
